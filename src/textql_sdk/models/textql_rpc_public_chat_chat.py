"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .textql_rpc_public_chat_chatsource import TextqlRPCPublicChatChatSource
from .textql_rpc_public_chat_llmmodel import TextqlRPCPublicChatLlmModel
from .textql_rpc_public_chat_methodology import TextqlRPCPublicChatMethodology
from .textql_rpc_public_paradigm_paradigm import (
    TextqlRPCPublicParadigmParadigm,
    TextqlRPCPublicParadigmParadigmTypedDict,
)
from datetime import datetime
import pydantic
from pydantic import model_serializer
from textql_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class TextqlRPCPublicChatChatTypedDict(TypedDict):
    id: NotRequired[str]
    r"""UUID"""
    paradigm: NotRequired[TextqlRPCPublicParadigmParadigmTypedDict]
    r"""ChatParadigm includes paradigm options"""
    model: NotRequired[TextqlRPCPublicChatLlmModel]
    timestamp: NotRequired[datetime]
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """
    org_id: NotRequired[str]
    member_id: NotRequired[str]
    summary: NotRequired[Nullable[str]]
    playbook_id: NotRequired[Nullable[str]]
    research: NotRequired[Nullable[bool]]
    r"""whether this chat was created with report mode enabled"""
    creator_email: NotRequired[Nullable[str]]
    api_key_client_id: NotRequired[Nullable[str]]
    updated_at: NotRequired[datetime]
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """
    is_bookmarked: NotRequired[Nullable[bool]]
    r"""whether this chat is bookmarked by the user"""
    preferred_provider: NotRequired[Nullable[str]]
    template_data_id: NotRequired[Nullable[str]]
    r"""UUID reference to playbook_template_data"""
    batch_run_id: NotRequired[Nullable[str]]
    r"""UUID reference to playbook_batch_run"""
    preview: NotRequired[Nullable[str]]
    r"""preview/snippet of first message content"""
    dashboard_mode: NotRequired[Nullable[bool]]
    r"""whether this chat was created with dashboard mode enabled"""
    source: NotRequired[TextqlRPCPublicChatChatSource]
    methodology: NotRequired[TextqlRPCPublicChatMethodology]
    is_running: NotRequired[Nullable[bool]]
    r"""whether this chat has an active stream"""
    is_unread: NotRequired[Nullable[bool]]
    r"""whether updated_at > last_read_at for the requesting user"""
    vllm_model_id: NotRequired[Nullable[str]]
    r"""vllm_model_id is the per-chat model identifier for the org's vLLM endpoint.
    Only meaningful when model == MODEL_VLLM.
    """
    fast_mode: NotRequired[Nullable[bool]]
    r"""fast_mode enables Anthropic's fast inference mode (speed: \"fast\").
    Supported on Opus 5 and Opus 4.8. Pricing is 2x standard rates.
    """
    agent_id: NotRequired[Nullable[str]]
    r"""Agent that owns this chat, unset for human-initiated chats."""
    agent_name: NotRequired[Nullable[str]]
    agent_profile_image_url: NotRequired[Nullable[str]]
    max_thinking: NotRequired[Nullable[bool]]
    r"""max_thinking runs extended thinking at max effort with visible reasoning.
    Supported on Sonnet 5, Fable 5, Fable 5.1, Opus 4.8, Opus 5, and GPT 5.6 Sol/Terra/Luna.
    Thinking tokens bill as output tokens.
    """


class TextqlRPCPublicChatChat(BaseModel):
    id: Optional[str] = None
    r"""UUID"""

    paradigm: Optional[TextqlRPCPublicParadigmParadigm] = None
    r"""ChatParadigm includes paradigm options"""

    model: Optional[TextqlRPCPublicChatLlmModel] = None

    timestamp: Optional[datetime] = None
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    org_id: Annotated[Optional[str], pydantic.Field(alias="orgId")] = None

    member_id: Annotated[Optional[str], pydantic.Field(alias="memberId")] = None

    summary: OptionalNullable[str] = UNSET

    playbook_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="playbookId")
    ] = UNSET

    research: OptionalNullable[bool] = UNSET
    r"""whether this chat was created with report mode enabled"""

    creator_email: Annotated[
        OptionalNullable[str], pydantic.Field(alias="creatorEmail")
    ] = UNSET

    api_key_client_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="apiKeyClientId")
    ] = UNSET

    updated_at: Annotated[Optional[datetime], pydantic.Field(alias="updatedAt")] = None
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    is_bookmarked: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="isBookmarked")
    ] = UNSET
    r"""whether this chat is bookmarked by the user"""

    preferred_provider: Annotated[
        OptionalNullable[str], pydantic.Field(alias="preferredProvider")
    ] = UNSET

    template_data_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="templateDataId")
    ] = UNSET
    r"""UUID reference to playbook_template_data"""

    batch_run_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="batchRunId")
    ] = UNSET
    r"""UUID reference to playbook_batch_run"""

    preview: OptionalNullable[str] = UNSET
    r"""preview/snippet of first message content"""

    dashboard_mode: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="dashboardMode")
    ] = UNSET
    r"""whether this chat was created with dashboard mode enabled"""

    source: Optional[TextqlRPCPublicChatChatSource] = None

    methodology: Optional[TextqlRPCPublicChatMethodology] = None

    is_running: Annotated[OptionalNullable[bool], pydantic.Field(alias="isRunning")] = (
        UNSET
    )
    r"""whether this chat has an active stream"""

    is_unread: Annotated[OptionalNullable[bool], pydantic.Field(alias="isUnread")] = (
        UNSET
    )
    r"""whether updated_at > last_read_at for the requesting user"""

    vllm_model_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="vllmModelId")
    ] = UNSET
    r"""vllm_model_id is the per-chat model identifier for the org's vLLM endpoint.
    Only meaningful when model == MODEL_VLLM.
    """

    fast_mode: Annotated[OptionalNullable[bool], pydantic.Field(alias="fastMode")] = (
        UNSET
    )
    r"""fast_mode enables Anthropic's fast inference mode (speed: \"fast\").
    Supported on Opus 5 and Opus 4.8. Pricing is 2x standard rates.
    """

    agent_id: Annotated[OptionalNullable[str], pydantic.Field(alias="agentId")] = UNSET
    r"""Agent that owns this chat, unset for human-initiated chats."""

    agent_name: Annotated[OptionalNullable[str], pydantic.Field(alias="agentName")] = (
        UNSET
    )

    agent_profile_image_url: Annotated[
        OptionalNullable[str], pydantic.Field(alias="agentProfileImageUrl")
    ] = UNSET

    max_thinking: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="maxThinking")
    ] = UNSET
    r"""max_thinking runs extended thinking at max effort with visible reasoning.
    Supported on Sonnet 5, Fable 5, Fable 5.1, Opus 4.8, Opus 5, and GPT 5.6 Sol/Terra/Luna.
    Thinking tokens bill as output tokens.
    """

    @model_serializer(mode="wrap")
    def _serialize_model(self, handler):
        optional_fields = set(
            [
                "id",
                "paradigm",
                "model",
                "timestamp",
                "orgId",
                "memberId",
                "summary",
                "playbookId",
                "research",
                "creatorEmail",
                "apiKeyClientId",
                "updatedAt",
                "isBookmarked",
                "preferredProvider",
                "templateDataId",
                "batchRunId",
                "preview",
                "dashboardMode",
                "source",
                "methodology",
                "isRunning",
                "isUnread",
                "vllmModelId",
                "fastMode",
                "agentId",
                "agentName",
                "agentProfileImageUrl",
                "maxThinking",
            ]
        )
        nullable_fields = set(
            [
                "summary",
                "playbookId",
                "research",
                "creatorEmail",
                "apiKeyClientId",
                "isBookmarked",
                "preferredProvider",
                "templateDataId",
                "batchRunId",
                "preview",
                "dashboardMode",
                "isRunning",
                "isUnread",
                "vllmModelId",
                "fastMode",
                "agentId",
                "agentName",
                "agentProfileImageUrl",
                "maxThinking",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    TextqlRPCPublicChatChat.model_rebuild()
except NameError:
    pass
