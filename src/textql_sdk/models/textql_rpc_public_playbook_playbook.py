"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .textql_rpc_identity_memberpreview import (
    TextqlRPCIdentityMemberPreview,
    TextqlRPCIdentityMemberPreviewTypedDict,
)
from .textql_rpc_paradigm_params_paradigmtype import TextqlRPCParadigmParamsParadigmType
from .textql_rpc_public_chat_llmmodel import TextqlRPCPublicChatLlmModel
from .textql_rpc_public_config_source_configsource import (
    TextqlRPCPublicConfigSourceConfigSource,
    TextqlRPCPublicConfigSourceConfigSourceTypedDict,
)
from .textql_rpc_public_paradigm_paradigmoptions import (
    TextqlRPCPublicParadigmParadigmOptions,
    TextqlRPCPublicParadigmParadigmOptionsTypedDict,
)
from .textql_rpc_public_playbook_chataccessmode import (
    TextqlRPCPublicPlaybookChatAccessMode,
)
from .textql_rpc_public_playbook_chataccessoverride import (
    TextqlRPCPublicPlaybookChatAccessOverride,
    TextqlRPCPublicPlaybookChatAccessOverrideTypedDict,
)
from .textql_rpc_public_playbook_filtercondition import (
    TextqlRPCPublicPlaybookFilterCondition,
    TextqlRPCPublicPlaybookFilterConditionTypedDict,
)
from .textql_rpc_public_playbook_playbookreportstyle import (
    TextqlRPCPublicPlaybookPlaybookReportStyle,
)
from .textql_rpc_public_playbook_playbookstatus import (
    TextqlRPCPublicPlaybookPlaybookStatus,
)
from .textql_rpc_public_playbook_playbooktriggertype import (
    TextqlRPCPublicPlaybookPlaybookTriggerType,
)
from datetime import datetime
import pydantic
from pydantic import model_serializer
from textql_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class TextqlRPCPublicPlaybookPlaybookTypedDict(TypedDict):
    id: NotRequired[str]
    r"""UUID"""
    org_id: NotRequired[str]
    member_id: NotRequired[Nullable[str]]
    connector_id: NotRequired[Nullable[int]]
    r"""Deprecated: use connector_ids instead"""
    name: NotRequired[str]
    prompt: NotRequired[str]
    created_at: NotRequired[datetime]
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """
    updated_at: NotRequired[datetime]
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """
    status: NotRequired[TextqlRPCPublicPlaybookPlaybookStatus]
    trigger_type: NotRequired[TextqlRPCPublicPlaybookPlaybookTriggerType]
    cron_string: NotRequired[Nullable[str]]
    dataset_ids: NotRequired[List[str]]
    r"""List of UUIDs"""
    reference_report_id: NotRequired[Nullable[str]]
    latest_chat_id: NotRequired[Nullable[str]]
    email_addresses: NotRequired[List[str]]
    slack_channel_id: NotRequired[Nullable[str]]
    tagged_slack_user_ids: NotRequired[List[str]]
    llm_model: NotRequired[TextqlRPCPublicChatLlmModel]
    paradigm_options: NotRequired[TextqlRPCPublicParadigmParadigmOptionsTypedDict]
    paradigm_type: NotRequired[TextqlRPCParadigmParamsParadigmType]
    is_running: NotRequired[Nullable[bool]]
    owner: NotRequired[TextqlRPCIdentityMemberPreviewTypedDict]
    has_write_permission: NotRequired[Nullable[bool]]
    is_subscribed: NotRequired[Nullable[bool]]
    report_output_style: NotRequired[TextqlRPCPublicPlaybookPlaybookReportStyle]
    template_header_id: NotRequired[Nullable[str]]
    r"""UUID reference to PlaybookTemplateHeader"""
    selected_template_data_ids: NotRequired[List[str]]
    r"""List of UUIDs for selected template data rows"""
    max_concurrent_templates: NotRequired[Nullable[int]]
    r"""Maximum number of templates to run concurrently"""
    auto_optimize_concurrency: NotRequired[Nullable[bool]]
    r"""Auto-optimize concurrency based on runtime"""
    connector_ids: NotRequired[List[int]]
    template_data_filters: NotRequired[
        List[TextqlRPCPublicPlaybookFilterConditionTypedDict]
    ]
    r"""Filter conditions for template data"""
    parent_playbook_id: NotRequired[Nullable[str]]
    origin_playbook_id: NotRequired[Nullable[str]]
    duplicated_by_member_id: NotRequired[Nullable[str]]
    duplicated_at: NotRequired[datetime]
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """
    chat_access_mode: NotRequired[TextqlRPCPublicPlaybookChatAccessMode]
    chat_access_overrides: NotRequired[
        List[TextqlRPCPublicPlaybookChatAccessOverrideTypedDict]
    ]
    chat_is_public: NotRequired[bool]
    dashboard_ids: NotRequired[List[str]]
    r"""List of dashboard UUIDs attached to this playbook"""
    recipient_email_column: NotRequired[Nullable[str]]
    r"""Column name in template data to use as recipient email override"""
    teams_channel_id: NotRequired[Nullable[str]]
    tagged_teams_user_aad_ids: NotRequired[List[str]]
    config_source: NotRequired[TextqlRPCPublicConfigSourceConfigSourceTypedDict]
    r"""Config-management metadata (provenance + reconcile state) for any file-based
    object. Presence on an object ⇒ the object is config-managed.
    """


class TextqlRPCPublicPlaybookPlaybook(BaseModel):
    id: Optional[str] = None
    r"""UUID"""

    org_id: Annotated[Optional[str], pydantic.Field(alias="orgId")] = None

    member_id: Annotated[OptionalNullable[str], pydantic.Field(alias="memberId")] = (
        UNSET
    )

    connector_id: Annotated[
        OptionalNullable[int],
        pydantic.Field(
            deprecated="warning: ** DEPRECATED ** - This will be removed in a future release, please migrate away from it as soon as possible.",
            alias="connectorId",
        ),
    ] = UNSET
    r"""Deprecated: use connector_ids instead"""

    name: Optional[str] = None

    prompt: Optional[str] = None

    created_at: Annotated[Optional[datetime], pydantic.Field(alias="createdAt")] = None
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    updated_at: Annotated[Optional[datetime], pydantic.Field(alias="updatedAt")] = None
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    status: Optional[TextqlRPCPublicPlaybookPlaybookStatus] = None

    trigger_type: Annotated[
        Optional[TextqlRPCPublicPlaybookPlaybookTriggerType],
        pydantic.Field(alias="triggerType"),
    ] = None

    cron_string: Annotated[
        OptionalNullable[str], pydantic.Field(alias="cronString")
    ] = UNSET

    dataset_ids: Annotated[Optional[List[str]], pydantic.Field(alias="datasetIds")] = (
        None
    )
    r"""List of UUIDs"""

    reference_report_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="referenceReportId")
    ] = UNSET

    latest_chat_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="latestChatId")
    ] = UNSET

    email_addresses: Annotated[
        Optional[List[str]], pydantic.Field(alias="emailAddresses")
    ] = None

    slack_channel_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="slackChannelId")
    ] = UNSET

    tagged_slack_user_ids: Annotated[
        Optional[List[str]], pydantic.Field(alias="taggedSlackUserIds")
    ] = None

    llm_model: Annotated[
        Optional[TextqlRPCPublicChatLlmModel], pydantic.Field(alias="llmModel")
    ] = None

    paradigm_options: Annotated[
        Optional[TextqlRPCPublicParadigmParadigmOptions],
        pydantic.Field(alias="paradigmOptions"),
    ] = None

    paradigm_type: Annotated[
        Optional[TextqlRPCParadigmParamsParadigmType],
        pydantic.Field(alias="paradigmType"),
    ] = None

    is_running: Annotated[OptionalNullable[bool], pydantic.Field(alias="isRunning")] = (
        UNSET
    )

    owner: Optional[TextqlRPCIdentityMemberPreview] = None

    has_write_permission: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="hasWritePermission")
    ] = UNSET

    is_subscribed: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="isSubscribed")
    ] = UNSET

    report_output_style: Annotated[
        Optional[TextqlRPCPublicPlaybookPlaybookReportStyle],
        pydantic.Field(alias="reportOutputStyle"),
    ] = None

    template_header_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="templateHeaderId")
    ] = UNSET
    r"""UUID reference to PlaybookTemplateHeader"""

    selected_template_data_ids: Annotated[
        Optional[List[str]], pydantic.Field(alias="selectedTemplateDataIds")
    ] = None
    r"""List of UUIDs for selected template data rows"""

    max_concurrent_templates: Annotated[
        OptionalNullable[int], pydantic.Field(alias="maxConcurrentTemplates")
    ] = UNSET
    r"""Maximum number of templates to run concurrently"""

    auto_optimize_concurrency: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="autoOptimizeConcurrency")
    ] = UNSET
    r"""Auto-optimize concurrency based on runtime"""

    connector_ids: Annotated[
        Optional[List[int]], pydantic.Field(alias="connectorIds")
    ] = None

    template_data_filters: Annotated[
        Optional[List[TextqlRPCPublicPlaybookFilterCondition]],
        pydantic.Field(alias="templateDataFilters"),
    ] = None
    r"""Filter conditions for template data"""

    parent_playbook_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="parentPlaybookId")
    ] = UNSET

    origin_playbook_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="originPlaybookId")
    ] = UNSET

    duplicated_by_member_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="duplicatedByMemberId")
    ] = UNSET

    duplicated_at: Annotated[
        Optional[datetime], pydantic.Field(alias="duplicatedAt")
    ] = None
    r"""A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

    Timestamp timestamp;
    timestamp.set_seconds(time(NULL));
    timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

    struct timeval tv;
    gettimeofday(&tv, NULL);

    Timestamp timestamp;
    timestamp.set_seconds(tv.tv_sec);
    timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
    Timestamp timestamp;
    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

    long millis = System.currentTimeMillis();

    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
    .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

    Instant now = Instant.now();

    Timestamp timestamp =
    Timestamp.newBuilder().setSeconds(now.getEpochSecond())
    .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

    timestamp = Timestamp()
    timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\" 
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    \"Z\") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    chat_access_mode: Annotated[
        Optional[TextqlRPCPublicPlaybookChatAccessMode],
        pydantic.Field(alias="chatAccessMode"),
    ] = None

    chat_access_overrides: Annotated[
        Optional[List[TextqlRPCPublicPlaybookChatAccessOverride]],
        pydantic.Field(alias="chatAccessOverrides"),
    ] = None

    chat_is_public: Annotated[Optional[bool], pydantic.Field(alias="chatIsPublic")] = (
        None
    )

    dashboard_ids: Annotated[
        Optional[List[str]], pydantic.Field(alias="dashboardIds")
    ] = None
    r"""List of dashboard UUIDs attached to this playbook"""

    recipient_email_column: Annotated[
        OptionalNullable[str], pydantic.Field(alias="recipientEmailColumn")
    ] = UNSET
    r"""Column name in template data to use as recipient email override"""

    teams_channel_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="teamsChannelId")
    ] = UNSET

    tagged_teams_user_aad_ids: Annotated[
        Optional[List[str]], pydantic.Field(alias="taggedTeamsUserAadIds")
    ] = None

    config_source: Annotated[
        Optional[TextqlRPCPublicConfigSourceConfigSource],
        pydantic.Field(alias="configSource"),
    ] = None
    r"""Config-management metadata (provenance + reconcile state) for any file-based
    object. Presence on an object ⇒ the object is config-managed.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "id",
                "orgId",
                "memberId",
                "connectorId",
                "name",
                "prompt",
                "createdAt",
                "updatedAt",
                "status",
                "triggerType",
                "cronString",
                "datasetIds",
                "referenceReportId",
                "latestChatId",
                "emailAddresses",
                "slackChannelId",
                "taggedSlackUserIds",
                "llmModel",
                "paradigmOptions",
                "paradigmType",
                "isRunning",
                "owner",
                "hasWritePermission",
                "isSubscribed",
                "reportOutputStyle",
                "templateHeaderId",
                "selectedTemplateDataIds",
                "maxConcurrentTemplates",
                "autoOptimizeConcurrency",
                "connectorIds",
                "templateDataFilters",
                "parentPlaybookId",
                "originPlaybookId",
                "duplicatedByMemberId",
                "duplicatedAt",
                "chatAccessMode",
                "chatAccessOverrides",
                "chatIsPublic",
                "dashboardIds",
                "recipientEmailColumn",
                "teamsChannelId",
                "taggedTeamsUserAadIds",
                "configSource",
            ]
        )
        nullable_fields = set(
            [
                "memberId",
                "connectorId",
                "cronString",
                "referenceReportId",
                "latestChatId",
                "slackChannelId",
                "isRunning",
                "hasWritePermission",
                "isSubscribed",
                "templateHeaderId",
                "maxConcurrentTemplates",
                "autoOptimizeConcurrency",
                "parentPlaybookId",
                "originPlaybookId",
                "duplicatedByMemberId",
                "recipientEmailColumn",
                "teamsChannelId",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    TextqlRPCPublicPlaybookPlaybook.model_rebuild()
except NameError:
    pass
